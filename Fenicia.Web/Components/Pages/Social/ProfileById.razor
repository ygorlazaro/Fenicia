@page "/socialnetwork/profile/{Id:guid}"
@inject NavigationManager NavigationManager

@code {
    [Parameter] public Guid Id { get; set; }

    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo($"/profile/{Id}", true);
    }
}